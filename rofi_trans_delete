#!/usr/bin/env bash

# configure
pattern="$@"

touch $transHistory

# delete pattern
if [[ -n $pattern ]]; then
    his="$(cat $transHistory)"
    echo "$his" | sed "/^${pattern}$/d" > $transHistory
fi

# print history
tac $transHistory
